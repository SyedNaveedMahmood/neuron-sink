# Tests

Add parity tests before interpreting new neuron-level results. Minimum checks should verify baseline sink/CE parity with the pinned upstream harness, identity behavior at `alpha=1`, deterministic selection under a fixed seed, and probability/shape invariants for intervention hooks.
