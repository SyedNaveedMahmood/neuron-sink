# Configs

Project-level experiment configuration goes here. Keep upstream paper configuration inside the pinned submodules unchanged; new configs should explicitly record source checkpoint, seed, dataset manifest, layer/head scope, neuron selection method, control type, and suppression strength.
