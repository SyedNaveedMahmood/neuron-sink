profiles:
  dev_rtx2060_family:
    role: implementation_and_smoke
    # Amendment A001 (docs/AMENDMENTS.md) registered a second development GPU. Both are
    # enforced by neuron_sink.provenance.REGISTERED_GPUS["dev"], which is authoritative.
    gpu:
      - NVIDIA GeForce RTX 2060 SUPER   # 8 GB, produced Task 1-3
      - NVIDIA GeForce RTX 2060         # 12 GB, produced Task 4 onward
    vram_gb: 8   # smoke policy is sized for the smaller card and is NOT relaxed on the 12 GB one
    primary_models:
      - openai-community/gpt2
    optional_models:
      - Qwen/Qwen2.5-0.5B-Instruct
    attribution:
      batch_size: 1
      layerwise_backward: true
      retain_all_layer_graphs: false
    phenomenon_smoke:
      discovery_examples: 24
      validation_examples: 24
      test_examples: 24
      sequence_length: 40
      neuron_fractions_percent: [0.05, 0.10, 0.25]
      alpha: [1.0, 0.5, 0.0]
      matched_random_draws: 5
    benchmark_adapter_smoke:
      examples_per_task: 20
      conditions: [B0, T1]
    note: >-
      Development results are not paper statistics. Do not change scientific definitions
      to fit 8 GB; reduce batch size and process layers serially instead. The 12 GB card
      registered by amendment A001 does not license a larger smoke experiment: the
      examples, fractions, alphas, and control counts above are unchanged on both GPUs.

  full_rtx4080_super:
    role: confirmatory_and_benchmark_runs
    gpu: NVIDIA RTX 4080 SUPER
    vram_gb: 16
    mechanism_models:
      - openai-community/gpt2
      - openai-community/gpt2-medium
    task_model:
      - Qwen/Qwen2.5-1.5B-Instruct
    gpt2_dtype: float32
    qwen_dtype: bfloat16
    phenomenon_full:
      discovery_examples: 100
      validation_examples: 100
      test_examples: 100
      sequence_length: 40
      neuron_fractions_percent: [0.01, 0.05, 0.10, 0.25, 0.50, 1.00]
      alpha: [1.00, 0.75, 0.50, 0.25, 0.00]
      matched_random_draws: 20
    downstream_conditions: [B0, T1, T2, R1]
    downstream_tasks:
      - mmlu
      - arc_challenge
      - culturalbench_easy
      - culturalbench_hard
      - gsm8k
    note: >-
      Run full downstream tasks only after the exact task checkpoint independently passes
      the neutral-corpus sink and targeted-neuron causal gates.
