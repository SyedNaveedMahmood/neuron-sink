# Neuron Localization

Placeholder for the first new implementation. Initial target: GPT-2-small, using the existing sink metric and sink-heavy layer/head information from `upstream/sink-repro`.

Planned output: ranked candidate MLP neurons and attention dimensions with enough metadata to reproduce the ranking. Do not treat attribution/ranking alone as causal evidence.
