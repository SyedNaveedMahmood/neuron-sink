# Scripts

Project-level entry points will go here. For the first implementation, prefer thin wrappers around pinned upstream evaluation/intervention code rather than duplicating whole upstream scripts.
